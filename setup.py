#!/usr/bin/env python
"""Minimal setup.py for backward compatibility."""
from setuptools import setup

# This setup.py is minimal and exists only for backward compatibility
# All configuration is in pyproject.toml
setup()
